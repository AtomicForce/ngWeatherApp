{
  "name": "ng-weather-app",
  "private": true,
  "version": "0.0.1",
  "devDependencies": {
    "bower": "^1.7.7"
  },
  "dependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "update-deps": "npm update",

    "prestart": "npm install",
    "start": "http-server ./ -p ${PORT:=8000} -c-1"
  }
}
